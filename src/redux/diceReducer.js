const initialState = {
    dice: [],
    choice: 0,
    match: 0,
    win: 0,
}
const diceReducer = (state = initialState, action) => {
    switch (action) {
        default:
            return state
    }
}
export default diceReducer;
